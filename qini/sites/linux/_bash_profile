# .bash_profile    --XXX having this avoids .profile 

# Get the aliases and functions
[ -f ~/.bashrc ] && . ~/.bashrc

# User specific environment and startup programs

# PATH=$PATH:$HOME/bin

export PATH
unset USERNAME
