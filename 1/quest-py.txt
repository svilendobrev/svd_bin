1   - lists / tuples / sets
1   - immutable / mutable
1   - str+str / ''.join()
1+  - == / is
1+  - if a then b else c / a and b or c / b if a else c
#1+ - range / xrange   ...py2
2   - if x / if x is True / if x == True
2   - what 'else' after 'for loop' means
2   - local / global / nonlocal / visibility - how to access/change things
2   - namespaces/visibility: 'a=1  class X: a=2  def f(): a=3  def g(): print a'
2+  - namespaces creating - module, func, class=tmp, generator-expr
2+  - class-attr vs inst-attr - access/visibility
3-+ - methods: instance-/ class- / static- ; bound/unbound 
2+  - zip / itertools.zip_longest  ...py2: / itertools.izip / itertools.izip_longest    
3   - dict.setdefault / dict.get / try dict[] except
3   - partial() / callable-obj
3   - properties
3   - classmethod / staticmethod
4   - howto make virtual class/static methods
4   - __new__ / __init__
4   - descriptors
5   - metaclass

# vim:ts=4:sw=4:expandtab
